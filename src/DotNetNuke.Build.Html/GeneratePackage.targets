<Project>

  <Target Name="GeneratePackage" AfterTargets="CoreCompile">
    <Message Importance="high" Text="Hello world from generate package!" />
    <Message Importance="high" Text="Project build dir: $(MSBuildProjectDir)" />
  </Target>

</Project>
